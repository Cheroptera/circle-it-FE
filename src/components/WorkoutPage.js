import React from 'react'
import { Timer } from './Timer'

//* This is the page where the user can see the current exercise, a timer and an image/video
export const WorkoutPage = () => {
  return (
    <div>
      I am a workout page
      <Timer />
    </div>
  )
}

import React from 'react'
import { Header } from 'lib/Header'

// * This is the page where the user selects what exercises to use
export const CustomWorkout = () => {
  return (
    <Header headerTitle="I am a custom workout!" />
  )
}